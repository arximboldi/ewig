
Ewig
====

Try it out
----------

Make sure that you have installed the `ncurses-dev` and `cmake`
packages. Then run:

```
git submodule update --recursive --init
mkdir build
cd build
cmake
make
```
